---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Portfolio - yoyo yuu">
    <div id="content-flex" class="flex justify-center items-start mt-4">
        <!-- Sidebar -->
        <div class="w-[180px] mr-4">
            <div class="sticky top-4 space-y-4">
                <!-- Stats -->
                <div
                    class="content-box border border-brand-pink p-3 text-center"
                >
                    <div class="text-2xl font-rock-bold text-brand-pink mb-1">
                        XX
                    </div>
                    <div class="text-sm">ilustraciones</div>
                </div>

                <!-- Categorias random -->
                <div class="content-box border border-brand-pink p-3">
                    <div
                        class="font-rock-bold text-brand-pink mb-2 text-center text-sm"
                    >
                        CATEGORÍAS
                    </div>
                    <div class="space-y-1">
                        <button
                            class="w-full text-left text-sm p-1 hover:text-blue-600"
                            >Destacados</button
                        >
                        <button
                            class="w-full text-left text-sm p-1 hover:text-blue-600"
                            >OCs</button
                        >
                        <button
                            class="w-full text-left text-sm p-1 hover:text-blue-600"
                            >Fanart</button
                        >
                        <button
                            class="w-full text-left text-sm p-1 hover:text-blue-600"
                            >Chibi</button
                        >
                        <button
                            class="w-full text-left text-sm p-1 hover:text-blue-600"
                            >Nose</button
                        >
                        <button
                            class="w-full text-left text-sm p-1 hover:text-blue-600"
                            >Sketch</button
                        >
                    </div>
                </div>

                <!-- Botoncito de comisiones -->
                <a
                    href="/comisiones"
                    class="block content-box border border-brand-pink p-3 text-center bg-[#ffb3d9] hover:bg-[#ff8cc7] transition-colors"
                >
                    <div class="font-bold text-[#7b0e55] text-sm">
                        Comisiones cerradas
                    </div>
                </a>
            </div>
        </div>

        <!-- Aqui va la galeria -->
        <div class="w-[900px]">
            <div class="content-box border border-brand-pink">
                <div
                    class="p-3 border-b border-brand-pink bg-brand-light-pink flex justify-between items-center"
                >
                    <h2 class="m-0 font-rock-bold text-brand-pink">PORTAFOLIO</h2>
                    <div class="text-sm">
                        Ordenar por:
                        <select
                            class="ml-2 border border-brand-pink rounded bg-white px-2 py-1"
                        >
                            <option>Más reciente</option>
                            <option>Más antiguo</option>
                        </select>
                    </div>
                </div>
                <div class="p-4 grid grid-cols-3 gap-4">
                    {
                        Array.from({ length: 9 }).map((_, i) => (
                            <div class="group cursor-pointer">
                                <div class="aspect-square border border-brand-pink/30 rounded overflow-hidden mb-2 group-hover:border-brand-pink transition-colors">
                                    <div class="h-full bg-gradient-to-br from-pink-50 to-purple-50 flex items-center justify-center group-hover:scale-105 transition-transform">
                                        <span class="text-4xl text-brand-pink/40 group-hover:text-brand-pink/60">
                                            :D
                                        </span>
                                    </div>
                                </div>
                                <div class="text-center">
                                    <div class="font-medium text-brand-pink">
                                        Título #{i + 1}
                                    </div>
                                    <div class="text-xs text-gray-500">
                                        Categoría • 2025
                                    </div>
                                </div>
                            </div>
                        ))
                    }
                </div>
            </div>
        </div>
    </div>
</BaseLayout>
